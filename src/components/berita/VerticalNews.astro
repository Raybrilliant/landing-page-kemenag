---
const {title, category, image, description} = Astro.props
---

 <div class="grid grid-cols-2 gap-5 items-center overflow-hidden">
    <img src={image} class="w-full object-cover rounded-xl" alt="gambar">
    <div class="flex flex-col gap-3">
        <div class="bg-amber-600 text-white rounded-full py-1 px-3 max-sm:text-sm w-fit">{category}</div>
        <h1 class="font-medium text-lg max-sm:text-sm">{title}</h1>
        <p class="opacity-50 text-sm text-justify max-sm:text-sm">{description.trim().slice(0, 90) + '...'} <a href="/" class="text-amber-600 hover:underline">Selengkapnya</a></p>
    </div>
</div>